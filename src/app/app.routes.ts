import { Routes as NgRoutes } from '@angular/router';

export const routes: NgRoutes = [
  { path: '', redirectTo: '/products', pathMatch: 'full' },
  // Remova temporariamente os outros caminhos até resolvermos os erros
];
